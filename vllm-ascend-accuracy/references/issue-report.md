# 精度问题报告模板

仅在准备提交 issue、缺陷单或研发交接材料时读取本文件。

## 摘要

- 现象：
- 影响：
- 发生率：失败数 / 请求数；测试轮次；置信信息（如适用）：
- 首次出现版本或变更：
- 已知正常基线：
- 最小触发条件：
- 可用规避方案及性能代价：

## 环境

粘贴 `collect_env.py` 的完整输出，并补充：

- vLLM Ascend、vLLM 的版本和 commit：
- 镜像 digest、CANN、driver/firmware、PyTorch、torch_npu：
- NPU 型号、单机卡数、节点数、互联方式：
- 模型/tokenizer 路径、revision、文件哈希、量化工具与配置：

删除凭据、内网地址、用户数据和无法公开的模型内容。

## 启动与请求

提供可复制的完整启动命令、相关环境变量和最小请求。附上 chat template 渲染结果或输入 token IDs、采样参数、stream 设置、并发与长度分布。

## 期望与实际结果

- 期望输出或参考后端结果：
- 实际原始响应：
- 输出 token IDs、`finish_reason`、`stop_reason`：
- 首个差异 token/step：
- 服务端关键日志：

长日志与大样本用附件或可访问的最小数据包，不要只贴截图。

## 消融结果

| 实验 | 唯一变化 | 请求数 | 失败数 | 分数 | 结论边界 |
| --- | --- | ---: | ---: | ---: | --- |
| 基线 |  |  |  |  |  |
| A |  |  |  |  |  |
| 回切 |  |  |  |  |  |

说明哪些事实已经复现，哪些仍是假设。若只在功能组合下触发，列出完整交互矩阵。

## 复现步骤

从干净的官方兼容环境开始，用编号命令说明：准备模型、启动服务、发送请求、统计失败。写清停止条件，避免无限压力循环。
